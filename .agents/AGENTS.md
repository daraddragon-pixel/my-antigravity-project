# Project Rules

- **Local Verification First**: Always implement changes and run the local static server first. Never commit or push changes to GitHub until the user has tested locally and explicitly approved pushing the changes in chat.
